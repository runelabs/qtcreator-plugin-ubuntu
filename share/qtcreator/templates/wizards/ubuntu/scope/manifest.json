{
@if "%ScopeType%" == "network"
  "description": "A simple Unity scope that accesses the network",
@else
  "description": "A simple Unity scope that accesses local content",
@endif
  "maintainer": "%ClickMaintainer%",
  "name": "%ClickDomain:l%.%ProjectName:l%",
  "title": "%ProjectName%",
  "hooks": {
    "%ClickHookName%": {
      "scope": "%ClickHookName%",
      "apparmor": "apparmor_%ClickHookName:l%.json"
    }
  }
}
